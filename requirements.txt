# Core — needed for every collector and the site build
pandas>=2.2
numpy>=1.26
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
jinja2>=3.1
python-dotenv>=1.0
pyyaml>=6.0
tenacity>=8.2

# Statistics for the total-loss-frequency nowcast (Project 3)
statsmodels>=0.14
scikit-learn>=1.4

# Browser automation for the inventory tracker (Project 1).
# After installing, also run:  python -m playwright install chromium
playwright>=1.44
